{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "chat slack project",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix app",
    "start": "start-server -s ./app/dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd app && npm ci"
  },
  "author": "Andrey",
  "license": "ISC"
}
